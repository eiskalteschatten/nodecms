{% for post in blogPosts %}
    <div class="uk-margin">
        <h3><a href="/blog/article/{{ post.slug }}">{{ post.name }}</a></h3>
        <p>{{ post.excerpt }}</p>
        {% if post.mediaFile %}
            <img src="{{ post.mediaFile.fileName | pathToUploads }}" alt="{{ post.name }}" title="{{ post.name }}">
        {% endif %}
    </div>
{% else %}
    <div>No blog posts were found.</div>
{% endfor %}
